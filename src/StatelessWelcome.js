import React from 'react';

const StatelessWelcome = ({counter}) => {
	return (
			<div>
				<h1>this is StatelessWelcome the counter value is {counter}</h1>
			</div>
		)
}

export default StatelessWelcome;
